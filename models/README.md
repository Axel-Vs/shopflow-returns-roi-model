# Models Directory

This directory stores trained model artifacts.

## Saved Models

- `random_forest_model.pkl` - Trained Random Forest model
- `logistic_regression_balanced_model.pkl` - Trained Logistic Regression model
- `preprocessor.pkl` - Data preprocessing pipeline

Files are automatically generated when running `enhanced_model.py`.
